#include "main.h"

/**
 * mul - multiplies to integers
 * @a: integer to be multiplied
 * @b: integer to be multiplied
 *
 * Return: always j
 */

int mul(int a, int b)
{
	int j;

	j = mul(a, b);
	return (j);
}
