#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @int: an integer input
 * Return: always 0
 */

int _abs(int)
{
	int r;

	if (r >= 0)
		return (r);
	else
		return (r * -1);

}
