#include "main.h"

/**
 * add -  adds two integers and returns the result
 * @x: an integer input
 * @y: an integer input
 * Return: Result to add x and y
*/

int add(int x, int y)
{
	return (x + y);
}
